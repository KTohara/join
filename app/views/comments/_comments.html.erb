<div>
  <% comments.each do |comment| %>
    <%= render 'comments/comment', comment: comment %>

    <% if comment.comments.exists? %>
      <div class="ml-5">
        <%= render 'comments/comments', comments: comment.comments.includes([:user, :commentable]) %>
      </div>
    <% end %>
  <% end %>
</div>