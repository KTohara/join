<% if commentable.class.name == 'Post' && commentable.post_comments_to_load.count >= 1 %>
  <%= button_to 'View More Comments', post_show_comments_path(commentable), id: "load_button_#{commentable.id }", class: 'hover:underline text-xs' %>
<% elsif commentable.class.name == 'Comment' && commentable.parent_comments_to_load.count >= 1 %>
  <%= button_to 'Show Replies', comment_show_comments_path(commentable), id: "load_button_#{commentable.id }", class: 'hover:underline text-xs' %>
<% end %>