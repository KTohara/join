<!DOCTYPE html>
<html>
  <head>
    <title>Join</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="current-user-id" content="<%= current_user&.id %>"> <%# current user conditionals when broadcasting turbo streams %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "https://kit.fontawesome.com/6191f9c23a.js", crossorigin: 'anonymous' %>
    <%= favicon_link_tag 'join-icon.svg' %>
  </head>

  <body class="font-roboto bg-stone-100 text-sm font-light overflow-x-hidden">
    <%= render 'shared/nav' %>
    <% if session[:chat_id].present? %>
      <%= turbo_frame_tag :chat, src: chat_path(session[:chat_id], chat_open: true) %>
    <% else %>
      <%= turbo_frame_tag :chat %>
    <% end %>
    <%= render 'shared/alert' %>
    <%= content_tag :main, class: main_layout do %>
      <%= yield %>
    <% end %>
  </body>
</html>