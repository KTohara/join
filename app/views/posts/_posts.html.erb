<div class="flex flex-col">
  <% feed.each do |post| %>
    <div class="border mt-2" id=<%= dom_id post %>>
      <%= link_to post.user.username, post.user %>
      <p><%= post.formatted_date %></p>
      <p><%= post.body %></p>
      <hr>
      <div class="flex gap-x-2">
        <div>
          <i class="far fa-thumbs-up"></i>
        </div>
        <div>
          <i class="far fa-pen-to-square"></i>
        </div>
        <div>
          <%= button_to post, method: :delete do %>
            <i class="far fa-trash-can"></i>
          <% end %>
        </div>
      </div>
      <hr>
      <span><%= pluralize(post.comments.map(&:comments).count, 'Comment') %></span>
      <%= render post.comments, max_nesting: 2 %>
      <%= render 'comments/form', commentable: post %>
    </div>
  <% end %>
</div>