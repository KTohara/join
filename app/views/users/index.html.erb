<h1>Search People</h1>

<%= render 'search' %>

<div>
  <% @users.each do |user| %>
    <div class="flex gap-x-2">
      <%= link_to user.username, user %>
      <%= render 'friendships/friendship_request', user: user %>
    </div>
  <% end %>
</div>